public class JobDistributingManager {
    // Singleton OR Static
    // Map with online encoders(ConnectionHandlers) and status (idle/busy)
    // List with inputs
    // String with the encoder attributes (command)
    // Algorithm to distribute and start file transfers to encoders
    // Getters/Setters OR public static
}
