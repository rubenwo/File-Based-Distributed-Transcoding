package ruben.distributed_transcoding.SlaveClient.CLI;

public class CommandLineInterface {
    // This will ask for ruben.distributed_transcoding.Server.ruben.distributed_transcoding.Server IP-Address
    // Then start the ruben.distributed_transcoding.SlaveClient.ruben.distributed_transcoding.SlaveClient as a Command Line Application
}
